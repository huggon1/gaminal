[project]
name = "terminal-monkey-type"
version = "0.1.0"
description = "A terminal typing speed test."
readme = "README.md"
requires-python = ">=3.12"
dependencies = ["textual>=0.65,<1.0"]

[tool.uv]
package = false
