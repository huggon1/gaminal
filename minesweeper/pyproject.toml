[project]
name = "terminal-minesweeper"
version = "0.1.0"
description = "A terminal minesweeper implementation for Linux."
readme = "README.md"
requires-python = ">=3.12"
dependencies = ["textual>=0.65,<1.0"]

[tool.uv]
package = false
